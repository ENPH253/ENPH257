function [ result ] = smoothData(dataset, steps)
%smoothData Performes a smoothing operation on the dataset, using an
%averaging operation over the stpes closest datapoints

[rows, columns] = size(dataset)


for row = 1:rows 
    for column = 1:columns
        
    end
end


